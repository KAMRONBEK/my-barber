# Google Maps API key — used by react-native-maps on both iOS and Android.
# Set this in your local environment before running `expo run:android` or `expo run:ios`.
# The key is injected into native config at prebuild time via app.json interpolation.
# Never commit the real key; this file is safe to commit.
GOOGLE_API_KEY=
