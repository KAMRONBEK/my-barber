# Google Maps API key — used by react-native-maps on both iOS and Android.
# Must be present in .env when running prebuild / native builds.
# After changing this key, rebuild native: pnpm --filter @my-barber/mobile ios
# Google Maps does NOT work in Expo Go on iOS — use a dev build (expo run:ios).
GOOGLE_API_KEY=
